# Research notes

These are the maintainer's own working notes, not pipeline inputs or outputs.
Nothing in `src/`, `scripts/` or `config/` reads either file.

- `belpulse_sources_open_data.csv` — a research table mapping Belfius's 2018
  indicator set to candidate open-data sources.
- `belpulse_gaps_regions.csv` — a research note on indicator coverage gaps by
  region.
