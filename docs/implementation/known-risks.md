# Known risks — redesign and page-builder work

Severity levels (from the maintainer's plan, unchanged):

- **P0** — data corruption, a security issue, a broken deployment, or an unusable route. Blocks
  immediately.
- **P1** — a wrong value, a missing required behaviour, or a major visual/accessibility failure.
  Blocks merge.
- **P2** — a minor defect or maintainability issue. Fix or explicitly document.
- **P3** — an optional improvement. Backlog.

## Open risks, tracked as they're found

| Date | Risk | Severity | Status |
|---|---|---|---|
| 2026-09-07 | No reference design files exist in the repo yet; Batches 1, 3, 4, 6, 7 cannot start without them | P1 (blocks those batches) | open — needs the maintainer |
| 2026-09-07 | The builder's serving technology (plain http.server vs a small framework) is not yet decided | P2 | open — recommendation in docs/features/page_builder.md |
| 2026-09-07 | "More data" (the maintainer's second ask alongside the redesign) has no specific sources named yet, so no docs/data_catalog.md row can be drafted (rule 8) | P2 | open — needs the maintainer |
| 2026-09-07 | Peer-group data bindings are specified ahead of Peer Model v1 (Block M) existing; the binding validator must reject them cleanly until then, not silently no-op | P2 | tracked in docs/features/data_binding.md |
| 2026-09-07 | `public/data/metadata/indicators.json` is MUNICIPAL-ONLY (52 rows). The 17 national indicators (GDP_ANNUAL_CY, HICP, UNEMPLOYMENT_RATE, …) exist only in `public/data/national.json` and appear in no metadata index at all. Any consumer treating the metadata index as "the list of indicators" silently drops every national figure | P1 | mitigated in Batch 9 (validator unions both files, with a regression test); the underlying asymmetry is still OPEN — national entries carry no `additive`, no `decimals`, no `grade`, and a MONOLINGUAL `name` string where municipal entries carry `names{en,fr,nl}`. Batches 3 and 6 (homepage, macro) will hit this; rule 7 says a national figure still needs a trilingual label |
| 2026-09-07 | `indicators.aggregation_method` is vestigial: unpublished, not declarable in YAML, and read by no exporter or analytic. 17 live indicators carry `'population_weighted'`, the exact method ADR 0003 measured as wrong and says "must not be added" | P2 | open — harmless while nothing reads it, which is why nothing may start to. Batch 9 corrected block_contract.md and data_binding.md, which both instructed a future implementer to enforce it. Cleanest real fix is dropping or defaulting the column, which needs its own migration and is not Batch 9's to do |
| 2026-09-07 | Batch 9's page-document validator has had NO adversarial security review. Its own hardening tests exist (`tests/security/test_page_document_hardening.py`) but no independent red-team pass | P2 | open, DELIBERATE — the attack surface (a builder HTTP API in Batch 11, binding resolution in Batch 14) does not exist yet, so a red-team pass now would attack a function with no caller. Recorded here so it cannot be quietly forgotten: **security-red-team must review `src/pages/` as part of Batch 11 AND Batch 14 acceptance**, not just the new code those batches add |
| 2026-09-07 | The page document format nearly shipped monolingual. Rev 1 of the Batch 9 spec never mentioned claude.md rule 7 at all; `seo.title`, `seo.description` and every accessible name would have been plain strings | P1 | CLOSED in Batch 9 (all user-facing strings are `{en,fr,nl}` objects, matching indicator_config.schema.json). Kept in this table on purpose: it was caught by an architecture review, not by a test or a rule check, and retrofitting it after Batches 10-15 wrote documents would have cost a schema_version bump plus a migration for every page |
| 2026-09-07 | **Process, not code.** Batch 9's page-document schema was written by three agents concurrently: two background implementers were still RUNNING when the worktree was inspected, found empty, and reported as failed, and `builder-core` was re-dispatched without confirming the first had terminated. Six files in `src/pages/` were overwritten mid-session and three substantive changes survived by last-writer-wins rather than by review | P1 | CLOSED for Batch 9 — both `builder-core` instances detected and reported the collision themselves, nothing was lost, the merged result was read as a whole and independently re-verified against the real payloads, and the one bad change (loose grid-overlap) was caught and overruled. Standing rule for the rest of the programme: **never infer "the agent failed" from an empty worktree while it is still running, and never re-dispatch without confirming termination.** On the highest-risk batch in the programme this came within one unreviewed edit of shipping a validator that passed silent mobile collisions |
| 2026-09-07 | The Batch 9 block registry declares 6 block types with `props_schema_status: provisional`, and deliberately omits `ranking_list` (needs Peer Model, Block M), `news_card` (no content source) and `simulated_live_counter` | P3 | open by design — Batch 10 may ADD registry keys and types, never change or remove Batch 9's. Recorded so Batch 10 knows the omissions were a choice, not an oversight |
| 2026-09-07 | `config/pages/{page_id}/draft.json` is a COMMITTED file in a repository whose root GitHub Pages serves, so once committed an unpublished draft is downloadable at the public URL. Invariant 10 still holds literally (nothing renders a draft, and the static export reads only `published.json`), but "a draft has no effect on the public site" is easy to read as "a draft is private", and it is not | P2 | open — found by the Batch 11 architecture pre-check, not by a test. Recorded in `config/pages/README.md` next to the files themselves. The real fix is either a `.nojekyll` + an explicit publish allowlist, or keeping drafts out of git entirely; both are decisions for the maintainer, and neither is Batch 11's to take unilaterally |
| 2026-09-07 | Playwright is not a declared dependency (absent from `pyproject.toml` dev extras and `requirements.txt`), so Batch 12's E2E tests — the batch's most important gate — must skip-guard and will **not run in CI**. They become documentation rather than a gate | P2 | CLOSED — `playwright>=1.62` in `pyproject.toml`'s dev extras, and CI runs `playwright install --with-deps chromium` after installing dependencies, since the package alone still skips. Merged into `feat/builder-shell`. Verified locally before/after on the 14 browser-gated tests in `tests/builder/test_builder_shell_service.py` (34 passed/14 skipped -> 48 passed/0 skipped); Batch 12b's `tests/builder/test_builder_shell_e2e.py` carries the same `ImportError` skip-guard and is now gated by CI too, which is the whole point — those E2E tests are the batch's real acceptance check |

| 2026-09-07 | Batch 12's spec, revision 1, described a builder shell that **could not load itself**: it required the session-token header on the `/app/*` static route while loading those files with `<script src>` / `<link rel=stylesheet>`, which cannot send a header, and `_check_token` accepts a query token on `/` only. Every subresource would have 401'd | P0 (spec-level) | CLOSED before implementation — the architecture pre-check caught it; revision 2 strikes the static route entirely and inlines the shell from `/`, which also removes the batch's largest piece of new attack surface. Recorded because it is the second time in this programme a pre-implementation architecture check, not a test, caught something a whole batch was about to be built on |
| 2026-09-07 | Batch 12's spec, revision 1, made byte-identical load→save round-tripping its headline acceptance property. It is false three ways by design: `/api/document` returns a MIGRATED document, `/api/save` canonicalises through `dumps(sort_keys=True)`, and an "unrecognised prop" cannot exist on disk (`additionalProperties: false`). An implementer would have built against a claim the code contradicts | P0 (spec-level) | CLOSED before implementation — restated in revision 2 as "canonical, unmigrated draft round-trips byte-identically; any migration or canonicalisation change is announced before the save, never silent". A related P1 with no defence anywhere in the pipeline is now a required test: the shell parses JSON in the **browser**, so `1.0`→`1`, `1e3`→`1000`, and integers above 2^53 lose precision |
| 2026-09-07 | Batch 11 recorded that a concurrent `save` from two tabs silently overwrites with no conflict detection (the document carries a `revision` field the service never reads). **Batch 12 changes the exposure, not the bug**: until now there was no UI to open twice | P2 | open — fix belongs to Batch 16 (publication transaction). Re-stated here because the severity of an unchanged gap can rise when something else ships |

| 2026-09-07 | `assets/belpulse/tokens.css` names `fonts.googleapis.com` and assumes the embedding page carries a Google Fonts `<link>`. The builder's preview iframe has no such link and the CSP blocks it, so preview typography falls back to IBM Plex Sans / system-ui instead of Inter. Correct behaviour under rule 30 (nothing the builder produces may require a network fetch to view), but **the canvas is not pixel-faithful on type**, and Batch 15's "compare the block version against the hand-built page" check will see a difference that is the preview's, not the page's | P2 | open — found during Batch 12a. Not fixable inside the builder without either embedding font files or allowing an external request; both are decisions above this batch. Whoever runs Batch 15's visual comparison must know the preview's type is a fallback before they diff screenshots |
| 2026-09-07 | A route-table comment in `src/builder/service.py` asserted that `/api/%73ave` 404s "rather than a second spelling of `/api/save`". False: the path is unquoted before comparison, so it resolves to `/api/save` and answers 405. The traversal defence itself is real and measured — only the worked example was wrong | P3 | CLOSED in Batch 12a (comment corrected, true behaviour pinned by a test). Recorded because a confident, wrong comment about a security control is how a later reviewer gets talked out of checking it: **security-red-team must attack the real behaviour, not this comment's old claim** |

| 2026-09-07 | The repository is public, and everything committed is downloadable from github.com as well as from Pages: `data/belgian_macro.db` (the canonical 18 MB database), `data/*.csv`, `config/`, `src/`, `tests/`, `scripts/`, `migrations/`, `docs/`. Verified: HTTP 200, 18,161,664 bytes, at both `https://mdruszcz.github.io/belgian-macro-pipeline/data/belgian_macro.db` and `https://github.com/mdruszcz/belgian-macro-pipeline/raw/develop/data/belgian_macro.db` | **REVIEWED AND ACCEPTED 2026-09-07** | accepted by the maintainer — this is an open-data project and the public repository and public data are intentional. **Kept here for one reason: no Pages-side change alters this.** A publication allowlist governs the site's URL surface only; anything committed to a public repo stays fetchable from github.com regardless. This row exists so nobody re-discovers the exposure, files it as a P0, and "fixes" it with a deploy change that produces a green build, a passing exclusion test and no change in reachability. That mistake was made once already while scoping Batch 17 (revision 1) and corrected before implementation |
| 2026-09-07 | **Rule 31 has no mechanical baseline.** Nothing in `tests/` asserts that any public URL resolves — the only `legacy` references there concern database tables and adapters, not routes. So "every existing canonical and legacy URL remains valid" is currently enforced by nobody, on a site of 1,695 commune pages in three languages plus 9 root pages | P1 | **CLOSED in Batch 17a.** `src/site/routes.py` is the inventory (1,711 URLs: 10 root, 1,695 commune, 6 block-built) and `tests/site/test_route_inventory.py` enforces it in both directions — every inventoried URL resolves, and every link a published page makes resolves. It went red on the day it was written and found three live 404s, all on the front page: `index.html`'s nav loaded `clock.html`, `news.html` and `analysis.html`, none of which were ever built, and `window.onload` opened `news.html` by default — **so the site's entry point had been loading a 404 into its own iframe on every visit**. The deploy change and the publication allowlist (Batch 17b, spec unchanged) still need the maintainer's Pages settings flip; the inventory is independently valuable and stays whatever happens to that |
| 2026-09-08 | `scripts/export_local_pages.py` gained a `src.pages` import in Batch 15c with no `sys.path` line, so it resolved under pytest (whose rootdir is already on the path) and **`make pages` died on `ModuleNotFoundError`** — `make all` was broken on `develop` with a fully green suite | P1 | CLOSED in Batch 17a. The path line is back, and `tests/site/test_route_inventory.py` now runs each exporter as a subprocess with `--help`, because a test that only ever imports a module cannot see this class of break |
| 2026-09-08 | The equivalence test for a converted page compared the built page against the LIVE hand-built file. That is correct only while the block version ships beside the original; the moment a page is cut over, the exporter overwrites that file and every assertion compares it to itself, passing while proving nothing | P1 | CLOSED in Batch 15d before the cutover: `tests/fixtures/pages/about-hand-built.html` is a frozen byte copy, committed first, and `test_about_conversion.py` compares against it. A test asserts the fixture is never regenerated and contains no block-renderer markup. Verified by sabotage — overwriting the fixture, and dropping one list item from the document, each go red |
| 2026-09-08 | The language switcher rendered INSIDE index.html's iframe and fought the parent: clicking "Français" navigated the frame, the new page posted `dashboard-ready`, the parent answered with its own unchanged `setLang('en')`, and the reader's choice snapped back to English — while the click had already written `belpulse-lang: fr`, so the page said one thing and storage said another | P1 | CLOSED in Batch 15d: `:root[data-framed] .bp-lang-switch{display:none}`. The parent owns the language when it owns the frame; two language controls in one viewport was the bug. Found by the independent audit, reproduced in a browser, and now covered by `tests/site/test_iframe_contract.py` in both directions |
| 2026-09-08 | The equivalence test for the converted page compared ENGLISH ONLY, and the trilingual assertion beside it checked one hero heading — not the six rich_text strings that are the rest of the page. Pasting the English paragraph into the `fr` key would have shipped an English French page with the whole suite green: the exact "drop two thirds of the audience invisibly" failure `docs/steps:899` names as the gate on this cutover | P1 | CLOSED in Batch 15d, found by the independent audit. The comparison is parameterised over all three languages against the frozen page's own translation table, plus a test that fails when a translated page still shows English. Verified by sabotage |
| 2026-09-08 | `translations_of()` asked about an already-translated route answered with URLs that do not exist (`/fr/about.html` → `/fr/fr/about.html`) | P2 | CLOSED in Batch 15d, found by the independent audit. Membership is tested against `declared_page_routes()` — the canonical route a document declares — rather than every published one. Unreachable from the sitemap today, which is why no test caught it |
| 2026-09-08 | `.bp-page` had no max-width, so every block-built page rendered edge to edge — ~1000px lines of body text on a laptop. Invisible to every test in the programme, which compares words rather than measure | P2 | CLOSED in Batch 15d: 1180px (the width home.html and the other redesigned pages already use) plus a 72ch reading measure on rich_text prose. Found by screenshotting the cutover, not by a test |
| 2026-09-08 | A block-built page loaded inside `index.html`'s iframe ignored the `setTheme`/`setLang` postMessages the hand-built page answered, and never posted `dashboard-ready` back — so the front page's tab would have sat frozen in one language and one theme, looking fine. The two theme vocabularies also do not overlap: index.html sends `day`/`soft`/`night`, the design system knows `light`/`dark` | P1 | CLOSED in Batch 15d: `src/pages/shell.py` implements the contract when framed, with `LEGACY_THEMES` naming the mapping and a guard against a navigation loop. `tests/site/test_iframe_contract.py` (10 tests) drives it in Chromium against a served copy of the repository — file:// would make the two frames opaque origins and test something else. **This row first claimed a browser test that did not exist**: the contract had been driven by hand in a scratch script, and the claim was written as though that were coverage. Caught by the independent audit `docs/steps:904` requires before a canonical cutover |
| 2026-09-08 | `builder/` (11 committed files: `builder/app/*.js`, `shell.css`) is served by Pages and is **absent from the Batch 17b spec's reconnaissance table**, which lists `src/ scripts/ tests/ migrations/ docs/ .claude/ .github/` | P2 | open — a default-deny allowlist excludes it automatically, so this is a gap in the spec's inventory rather than in the design. Recorded so whoever implements 17b does not discover it by having the builder shell vanish |
| 2026-09-08 | `shared-styles.css` is committed, served, and linked by **no page at all**, despite its own line 123 instructing that it be linked from `index.html` and `dashboard.html` | P3 | open — harmless, but it means an allowlist decision about it has no reader-visible consequence either way, and a future edit to it will change nothing while appearing to |

| 2026-09-07 | Open data is not the same as unattributed data. The repository redistributes source data (Statbel and others) whose licences carry attribution and compliance obligations, and those obligations are unaffected by the maintainer's decision that the files may be public — they are a different question, not the same one answered twice. This project has already had one attribution breach (Statbel, Block K) | P2 | open — **deliberately not scoped into Batch 17**, which is about the published site's URL surface, not about licence compliance. Recorded so it is not lost: needs its own licence pass covering every redistributed source, the database and CSV exports included, before any commercial use |

| 2026-09-07 | `route` is not an open-ended slug: `src/pages/semantics.py`'s allowlist (surfaced only through `POST /api/validate`'s own message, per the Batch 12 spec's own instruction not to copy it) is a small, closed set of **exact literal paths** already claimed by the existing site (`/`, `/about.html`, `/index.html`, `/local/{nis}/` and its `fr/`/`nl/` variants, etc.) plus the `{nis}` template. There is currently no "give me a new, unclaimed page" shape at all — the Batch 12 shell's new-page form can only create a draft for a route the allowlist already names | P2 | open — found while building and manually exercising Batch 12b's new-page form (not by a written test). Genuinely new pages (Batch 15's stated goal) need a route added to `src/pages/semantics.py` FIRST, which is out of this batch's scope and not something the builder can do for itself. Whoever plans Batch 15 needs to know this before assuming the builder alone can stand up a new URL |
| 2026-09-07 | There is no route to render a preview of an UNSAVED, in-memory document — `GET /preview` reads `store.read_document_text()` from disk, and no endpoint accepts a document body and returns rendered HTML without writing it. Batch 12b's canvas therefore shows the last explicitly-**saved** draft, not live keystrokes; the spec's "preview refresh, debounce after last keystroke ≤ 400 ms" performance limit is measured in the batch report as time-from-Save-click-to-canvas-update instead, and is stated as such rather than silently reinterpreted | P2 | open — a `POST`-with-body preview-render endpoint would fix this properly but is a `src/builder/service.py` change outside both 12a's and 12b's allowed edits (12b may only add filenames to `SHELL_JS_FILES`/`SHELL_CSS_FILES`). Flagged for whoever plans Batch 13, which already owns debounced autosave and would naturally also close this gap |
| 2026-09-07 | The batch spec requires warning **before save** when saving would change bytes even without a migration (a hand-edited draft with non-canonical key order/indentation). The shell has no way to detect this case specifically: `GET /api/document` returns the migrated, already-**parsed** structure, never the original file's raw bytes or a hash of them, so there is nothing on the browser side to diff against. Batch 12b's Save control instead carries a permanently visible caption stating plainly that Save always writes canonical JSON (sorted keys, 2-space indent, trailing newline) and that this changes the file's bytes if they were not already in that form — an honest standing disclosure, not a per-save dynamic diff | P2 | open — a real fix needs the service to expose either the pre-migration raw bytes or a canonical-vs-stored equality flag alongside `migrated`, which is a `src/builder/service.py` / API-shape decision outside 12b's scope. Flagged for whoever next touches `/api/document` |

| 2026-09-08 | Batch 12's keyboard-only accessibility test asserted `not browser.console_errors`, but Chromium logs every non-2xx fetch as a console error — including the 422 the service is SUPPOSED to answer when the 400 ms live-validate debounce fires mid-word on a half-typed form. Reproduced deterministically under CPU load; a GitHub runner is exactly that machine, so the batch's own accessibility gate would have gone red on the first PR for a reason unrelated to accessibility | P1 | CLOSED — the browser harness now ignores only the browser's own resource-load line for status 422, and only that; every other status still fails the test. Raw messages stay available on `Browser.all_console_errors` |
| 2026-09-08 | CI's `timeout-minutes: 10` could not accommodate the suite: 7m51 measured locally for tests alone, before `pip install -e ".[dev]"` and a cold runner's Chromium download plus its apt system libraries. The job would have been killed mid-run, and a timeout reads as a hang rather than as a budget | P1 | CLOSED — raised to 25 with the measured figures recorded beside it. Never observed before because ci.yml triggers only on `pull_request` and pushes to `develop`/`main`, and this work has lived on a feature branch with no PR |
| 2026-09-08 | The `api()` closure that attaches the session token refused `//host` but not `/\host`: the WHATWG URL parser treats a backslash as a separator for http(s), so `new URL("/\\evil.example", location.href)` resolves to `http://evil.example/` — cross-origin, with `X-BelPulse-Token` attached. Only `connect-src 'self'` actually stopped the request. Not exploitable while every path in `api.js` is a literal; it goes live the moment Batch 13/14 builds a path from data | P2 | CLOSED — the guard now rejects a backslash anywhere in the path. The test covering this asserted the SOURCE contained two substrings, which is how the hole survived it; replaced with a behavioural test that lifts the shipped condition out of the generated page, proves with the browser's own URL parser that the hostile paths really do leave the origin, then runs the guard against them |
| 2026-09-08 | Closing any confirmation dialog dropped keyboard focus to `<body>`: `closeModal` re-rendered the whole top bar, destroying the button that opened the dialog, and `store._releaseModalTrap` was written but never read — so every modal also leaked a keydown listener. A keyboard operator who cancelled a publish, restore or delete had to Tab through the entire shell to get back | P2 | CLOSED — the opener's id is captured by `renderAll` BEFORE anything is torn down (reading `document.activeElement` at dialog-build time is already too late, focus has fallen to `<body>` by then), restored after the re-render, and the trap is released. axe cannot see this class of defect: the markup is correct at every instant and only the transition loses focus |
| 2026-09-08 | The axe accessibility scan locates axe-core by globbing `/home/*/.npm/_npx/*/node_modules/axe-core/axe.min.js`, which does not exist on a CI runner — so the only automated accessibility check skips there and proves nothing. Separately, both `_chromium()` helpers catch bare `Exception` on launch and skip, so a broken browser install would make the whole browser gate green-and-absent rather than red | P2 | open — the auditor ran axe by hand against the panels the e2e test never reaches (new-page form, inspector with a block selected, all six inspector tabs, the open publish modal) and every one reported 0 serious/critical, so the gap is real but currently benign. The fix is declaring axe-core as a real dependency the way Playwright now is |

| 2026-09-08 | **Batch 14's resolver rendered a WITHHELD figure as a blank cell.** `_resolve_comparison` kept the value and discarded the state, so commune 11025 (Lint), whose `PART_TIME_BENEFIT_RECIPIENTS` is suppressed 2021-2026, sat beside real province, region and country numbers with an empty box — inside a block reporting `state: "ready"`. A figure ONEM withholds for privacy, presented as nothing at all | P0 | CLOSED — the subject's cell carries its state through a `_cell_text` helper and reads "withheld". THIS IS THE SAME REGRESSION docs/steps records as already shipped once ("Withheld figures are visible as withheld"), reintroduced on a different code path months later. `_resolve_latest` had it right the whole time; the comparison path did not. Rule 26 has now been broken twice in the same repository by two different authors, which says the rule needs a shared helper rather than repeated care |
| 2026-09-08 | **`indicator` and `nis` were unconstrained filesystem input on the deliberately-unvalidated preview path.** The schema constrains both, but `POST /api/preview` and `GET /preview` resolve documents the validator never saw — by design, so an invalid document can still be previewed. Demonstrated, not theorised: an ABSOLUTE nis silently replaces the whole prefix in pathlib (`Path("communes") / "/etc/passwd.json"` is `/etc/passwd.json`) and returned a value from outside the repository into the rendered preview; `indicator: "../manifest"` climbed out of `indicators/`; and the error messages formed an existence oracle for any path on the machine | P0 | CLOSED — both names are re-checked by regex INSIDE `PayloadReader`, plus a `resolve()`/`is_relative_to` containment check for a planted symlink, which no name check can see. Gated behind the token and Host/Origin checks so never remotely exploitable, but the constraint the resolver relied on did not exist on the path the resolver is actually invoked on. **That asymmetry is exactly what known-risks:22 reserved this review for, and it was found the first time anyone looked** |
| 2026-09-08 | Comparison rows took each aggregate's value while ignoring its own `period` and `coverage`: 53 mismatches in the first 150 payloads, some six years apart, and a "Belgium" total built from 488 of 529 communes printed as Belgium's figure with no caveat | P1 | CLOSED — every row carries its own period, a below-full-coverage row states its fraction in its own label, and a table mixing periods is flagged. Coverage was already in the payload; it was simply dropped |
| 2026-09-08 | `_format_value` matched only unit `"EUR"` and `"%"`, and treated `decimals: null` as zero. Real metadata uses `percent` (10 indicators), `eur` (5), `percent_per_year`, `per_10000_*`, and null decimals on 13. An 18.16% unemployment rate rendered as a bare `18` — a number a reader can only misread — and the batch's claim of showing "the same number the published site shows" was false for about a third of municipal indicators | P1 | CLOSED — mirrors `assets/commune_map.js`'s `formatValue`, which is what the live site uses: null decimals means two digits below a thousand, `unit.startsWith("percent")` appends %, `eur` prefixes €. Language-aware too, so a French preview groups as "35 363" |
| 2026-09-08 | A `national` binding was told its figure was "not published for this commune": every operation went to the commune payload regardless of provider, and `reader.national()` was dead code. The figure IS published, one directory up. The level refusal also swallowed `country`, which is the schema's own shape for a national binding, under a message describing province and region | P1 | CLOSED — national bindings read `national.json`, and the refusal names the level it is refusing and no longer covers `country` |
| 2026-09-08 | `history` ignored the binding's period entirely, so a chart authored for a 2015-2020 `range` — schema-valid and validator-accepted — silently drew every period in the payload | P1 | CLOSED |
| 2026-09-08 | A read error interpolated the exception, putting an absolute repository path into the data handed to the browser. It did not reach a reader only because the renderer prints `STATE_TEXT[state]` and never `data["message"]` — an accident of the renderer, not a control, against `service.py`'s own rule that nothing about this machine reaches the client | P2 | CLOSED — the message names the payload, never the exception |
| 2026-09-08 | A non-string `indicator` raised `TypeError: unhashable` out of `preview_data`, which catches only `PageDocumentError`, giving an opaque 500 on the one route whose whole purpose is rendering malformed documents | P2 | CLOSED — returns an `error` state for that block |
| 2026-09-08 | The rewritten Batch 11 preview test accepted `"error"` among its valid states, so it would have passed in a world where the resolver failed on every block — the outage it exists to catch | P3 | CLOSED — `"error"` dropped from the accepted set |
| 2026-09-11 | **Batch O's four municipal-finance ratios stopped every commune export on a database that had no WalStat rows yet.** `load_and_validate_derived` resolved a derived config's inputs against the indicators PRESENT IN THE STORE, so a config checked in beside its adapter — the normal order, since a ratio is config and the data arrives on the next daily run — raised `ConfigValidationError` out of `export_aggregates_csv.py`, `export_percentiles_csv.py` and `export_communes_history_csv.py`. Those three steps have no `continue-on-error` in the daily workflow and run BEFORE the sync step, so the first daily build after merge would have died before staging anything: no data PR that day, for every source, not just WalStat | P0 | CLOSED — a config whose inputs are configured but not yet in the store is now DEFERRED (left out of that export) rather than fatal, and a config naming an input that exists nowhere still fails loudly. Verified byte-identical: the committed export on the committed database is the same 36,455,348 bytes, same SHA, before and after. Found by the auditor, not by a test, because nothing ran the committed configs against the committed data |
| 2026-09-11 | Two WalStat rows carrying DIFFERENT NIS codes that resolve to the same commune-year (55010 and 51067 are both Enghien for 2013) were accepted silently: the duplicate check keyed the RAW code, the re-coding rule then mapped one onto the other, and `upsert_observation` wrote both — two vintages in one run, the later row winning, with no refusal and no count. The coverage check cannot see it, because it compares sets | P1 | CLOSED — resolution is deduplicated on the RESOLVED (geo_id, period) as well, and refuses with both codes named. Tested on the real Enghien pair |
| 2026-09-11 | The new `eur_per_inhabitant` unit was mirrored into two of the site's THREE value formatters: `assets/commune_map.js` and `src/pages/resolve.py` had it, `scripts/export_local_pages.py` (the permanent /local/{nis} pages) did not, so it fell through to the bare-number branch. Not yet visible — the section configs keep WalStat off those pages — but the moment Batch C lists a finance indicator, a debt of €2 965,5 per head would print as `2.965,50`: no euro sign, no "/ hab.", readable as a total | P2 | CLOSED — mirrored and tested in all three languages. THREE COPIES OF ONE FORMATTING RULE IS THE REAL RISK: this is the second unit to need the same change in three files |
| 2026-09-11 | The ratio tests called `growth_rate`/`share_of_total` directly on fixture series, so the CONFIG wiring — function name, input ORDER, the `args: {years: 1}` key — was never executed by any test. Swapping `MUN_DEBT_TO_REVENUE`'s inputs would have kept the suite green while the site published revenue as a share of debt. This is why the P0 above shipped | P2 | CLOSED — a test now runs the COMMITTED configs through the derived engine on the real fixture rows and asserts the same four hand-computed figures |
| 2026-09-11 | WalStat is NOT in the daily workflow yet, and the committed database carries its reference rows only. Loading the nine series takes `data/belgian_macro.db` from 19.8 MB to 33.9 MB (over CLAUDE.md rule 12's 25 MB) and `data/communes_history.csv` from 38.3 MB to 45.2 MB — past the workflows' own 40 MB commit tripwire, which would refuse to commit ANY data that day. The tripwire is doing its job: that CSV repeats every commune's five names, region, province and arrondissement on all 222,893 rows, which docs/steps already records as needing the treatment `export_percentiles_csv.py` got | P1 | CLOSED 2026-09-12 — trimmed rather than raised a third time. `export_communes_history_csv.py` now writes the last 10 calendar years by default (`--all-periods` for the rest, same flag name as `export_percentiles_csv.py`'s own precedent), applied AFTER the derived engine computes on the full series so a 10-year CAGR still sees its own ten-year-back input even though that row is trimmed from the output. `export_communes_table_json.py` and `export_site_payloads.py` — whose contract is the full history — now read a gitignored `data/communes_history_full.csv` instead, never the committed trimmed copy. Verified end to end on a live sync of a copy of the database: committed file 32.5 MB (was 19.7 MB before WalStat, would have been 45.2 MB untrimmed), full file 44.8 MB (over the guard, correctly never committed), Namur's `communes/92094.json` unaffected — still the complete 2013-2024 series. `sync_walstat` is now wired into `daily_fetch.yml` (continue-on-error, in the failure gate) with `fetch_window_days: 14` on the source |
| 2026-09-11 | Three of Block O's seven ratios are NOT built: personnel cost as a share of expenditure and any balance or structural balance. WalStat publishes neither personnel costs nor a balance, and a balance would need a new `difference` function in the derived engine — an analytical-formula change, which CLAUDE.md rule 19 puts behind an ADR. Investment is shipped as EXTRAORDINARY EXPENDITURE, which is the Belgian municipal accounting equivalent but is a substitution, not the same label | P2 | open — deferred deliberately; docs/steps' ratio step is marked partial with these three named. Needs an ADR plus a source that publishes the personnel line |

| 2026-09-12 | **`pytest-xdist` makes a pre-existing test-harness race land.** Every browser/API test file builds its own `ServerHarness` via a `_free_loopback_port()` copy that binds to port 0, reads the assigned port back, then CLOSES that probe socket before starting the real `BuilderServer` on the same number — a bind-after-close gap. Sequentially that gap is never contended. Under `-n auto` (measured: fast tier 91s -> 41s on 8 cores) two workers' probes can land on the same just-freed port inside that gap, and because `BuilderServer.allow_reuse_address = True` (`src/builder/service.py`), BOTH real servers bind successfully instead of one failing loudly with "address already in use" — requests then land on whichever socket the OS hands them. Reproduced twice: `test_post_with_no_origin_is_forbidden` and `test_post_with_other_loopback_port_origin_is_forbidden` both raised `ConnectionAbortedError [WinError 10053]` only under `-n auto`, never sequentially | P2 | open, deliberately not shipped — `make test` stays sequential; `pytest-xdist` remains a declared dev dependency for optional ad hoc use only (`pytest -n auto path/to/file.py`), where the collision window is negligible. A real fix means giving each harness copy a bind-time collision check (retry on `OSError` rather than trusting the probed number) across all four copies (`tests/builder/test_builder_api.py`, `test_builder_shell_e2e.py`, `test_builder_shell_service.py`, `tests/security/test_builder_service_hardening.py`) plus the iframe file's server — a `-n auto` default is worth revisiting once that lands, not before

| 2026-09-12 | **The committed SQLite database is the binding constraint on every future municipal series, and it is 1.2 MB from stopping the daily run.** `data/belgian_macro.db` is committed by `daily_fetch.yml` and sits at 37.9 MB against that workflow's own 39.06 MB guard (`find -size +40000k`); CLAUDE.md rule 12's stated ceiling is 25 MB, which it passed long ago. ONEM's monthly rate is capped at **18 months** for this reason alone and not for any data-quality reason: measured from the same baseline, 24 months gives 39.45 MB and fails the guard by 0.38 MB, 36 months gives 42.6 MB, and the full 114-month history gives 63.0 MB — 64,524 rows, 46 % of every observation in the store, for one indicator. So the next municipal source of any size cannot land at all without this being solved first | P1 | open — flagged, not fixed. Trimming the window is a workaround and is recorded as one in `scripts/sync_onem_rates.py`. **CORRECTED 2026-09-12, same day: my first diagnosis here was wrong.** I wrote that the fix was to stop committing a file the workflow rewrites from scratch daily. It does not — `daily_fetch.yml` checks the database out, applies pending migrations and APPENDS, and it is the only store holding the automated sources and every superseded vintage, so dropping it would lose data. Measured with `dbstat`, **62 % of the 37.9 MB is index**: 7.31 MB for the primary key's own B-tree plus 4.91 + 4.10 + 3.48 + 2.55 + 0.88 MB across five secondary indexes, against 13.60 MB of actual data. An index holds no information and the five rebuild in 0.68 s (measured, 86k rows) from DDL that is already `CREATE INDEX IF NOT EXISTS` in `migrations/002_indexes.sql`. **Not committing them gives 21.96 MB** — inside rule 12's 25 MB ceiling for the first time since WalStat landed — and 17.1 MB of headroom instead of 1.18 MB. Also measured and rejected: `WITHOUT ROWID` on `observations` makes it BIGGER (51.14 MB), because every secondary index then stores the four-column TEXT primary key as its row locator. And it reframes the window: not committing the indexes, the FULL 114-month history measures **35.89 MB and passes the guard** (63.04 MB with them, which fails), so the choice is not "18 months or break the run". What it takes is one step dropping the five before the commit plus a replay of `migrations/002_indexes.sql` at the start of each run — already `CREATE INDEX IF NOT EXISTS`, so idempotent by construction, and no data can be lost because an index is output, not input. Still the maintainer's call, but a far smaller change than the one I first proposed |
| 2026-09-12 | **The oversized-file guard failed runs over a file that is never committed.** Its message reads "refusing to commit", but it scanned `find data/ public/ local/` — which matched `data/communes_history_full.csv`, excluded by `.gitignore` and described by the step that builds it, in the same workflow, as "gitignored and never committed". Measured on run 34699560386 that file is ~37.5 MB against the 39.06 MB threshold, so it was ~1.5 MB from failing the daily run and throwing away every other source's data for the day. Adding ONEM's rate (+3.4 MB to that file) would have tripped it | P1 | CLOSED 2026-09-12 — the guard now skips paths `git check-ignore` matches, in both `daily_fetch.yml` and `manual_sources.yml`. **The threshold is unchanged and every committed file is still checked**: docs/steps' decision to fix the next overrun at the source rather than raise the guard a third time still stands, and the row above is that overrun |
